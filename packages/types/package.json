{
  "name": "@myfundingtrade/types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types, DTOs, and interfaces",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
